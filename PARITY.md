# Basalt — Obsidian core-app parity audit

_Point-in-time audit, 2026-07-07. Method: a multi-agent sweep — for each feature an Explore-based auditor located the implementation (with `file:symbol` evidence), then an adversarial skeptic challenged every "full" claim and confirmed every "missing" one. **Scope excludes** Obsidian Sync/Publish, the mobile app, and the community plugin/theme marketplaces — those are out of scope by design (see [ARCHITECTURE.md](./ARCHITECTURE.md)). Regenerate as features land; keep it honest._

## Verdict

Basalt clears the bar for a credible core-app foundation but not for full core-app parity: ~67% weighted parity, with 37 of 84 in-scope features at full parity and a striking 38 partials. The everyday note-taking loop — markdown Live Preview, wikilinks, backlinks, tags, full-text search, file tree, canvas, and properties — is present and mostly usable, so the pattern is breadth over depth: most features exist as thin implementations that stop well short of Obsidian's controls, panels, and configurability. The most serious holes are in data safety and interop: opening and editing any note silently rewrites CRLF to LF, non-UTF8 files are unreadable, and rename does not rewrite references inside .canvas/.base files (leaving dangling links) — real deviations from the "plain files, untouched" promise. There is also a concrete editor bug (internal [text](note.md) links do not navigate in edit mode, only in reading view) and four wholly missing core-plugin equivalents (note composer, random note, slides, audio recorder), plus no plugin-extensibility for settings tabs, ribbon, or status bar. Graph view, tables, canvas node types, and Bases are functional but shallow, so this reads as a strong pre-1.0 alternative that is close on surface coverage yet clearly behind on depth and safety guarantees.

**Weighted parity: ~67%** ((full + 0.5·partial) / in-scope features). Of 85 in-scope features: **37 full, 38 partial, 9 missing, 1 bonus**.

## Fixed since this audit (2026-07-07)

The four **HIGH**-severity items — the data-safety/interop holes and the one
confirmed bug — have been resolved (the matrix below still shows the pre-fix
state):

- ✅ **Internal markdown-links navigate in the editor.** `[text](Note.md)` now
  routes to the note-open handler in Live Preview, not the external opener.
- ✅ **CRLF line endings preserved.** Normalization moved to the disk boundary
  in Rust: `read_note`/`read_vault` return LF (so the whole frontend compares
  LF-vs-LF), `write_note`/`write_canvas`/`write_base` re-apply the file's
  existing EOL (majority-detected) — a CRLF file round-trips CRLF; new/LF files
  stay LF.
- ✅ **Rename rewrites canvas embeds.** Note and folder renames repoint
  `.canvas` file-node embeds (of moved notes *and* attachments) via a minimal,
  geometry-preserving JSON edit, case-insensitively. (Bases query dynamically,
  so they don't dangle on rename — no base rewrite needed.)
- ✅ **Non-UTF8 handling — deliberate safety stance, clarified.** `read_note`
  still refuses non-UTF8 rather than lossily re-encoding (which could corrupt a
  shared vault; Obsidian vaults are UTF-8), but now with a clear, actionable
  message. Full encoding conversion is intentionally *not* implemented — high
  risk, low value for a UTF-8 vault format.

Each fix landed with regression tests; the canvas/CRLF write-path changes went
through an adversarial data-safety review.

### Depth & UX gaps closed (2026-07-08)

A follow-up pass closed ~17 more MEDIUM/LOW gaps from the list below:

- **Editor/reading:** interactive task checkboxes in reading mode; `%%comments%%`
  hidden in Live Preview; per-type **callout icons** (LP + reading); hover
  page-preview on markdown-style links.
- **Links/backlinks/tags:** quick-switcher **"Create ‹query›"** + `[[` create-new
  entry; **backlinks grouped** by source file (collapsible); **"Link" / "Link
  all"** unlinked mentions; unresolved outgoing links clickable; **nested tag
  hierarchy** with roll-up counts.
- **Files/workspace:** **file-explorer sort** (name/modified/created); drag a
  note to the vault-name header to move it to **root**; sidebar collapse state
  persists.
- **Misc:** **random-note** command; status bar shows **character count**;
  templates honor the vault's `dateFormat`/`timeFormat`; CSS snippets also read
  `.obsidian/snippets`.

### More gaps closed (2026-07-08, second pass)

- **Editor:** **list/indent folding** (fold a list item's children); **inline
  raw HTML in Live Preview** (`<sup>`/`<sub>`/`<mark>`/… allowlist); **interactive
  table editor** (cell-precise click-to-edit + add/delete row/column controls);
  **Vim key bindings** (optional, live toggle); **RTL** editor direction.
- **Search/graph:** **`OR`** search operator (top-level OR-groups); local-graph
  **depth** control + global graph **filters** (name search + hide orphans).
- **Workspace:** **named workspaces** (save/switch/delete layouts); **resizable +
  properly-hidable sidebars** — the layout was a fixed CSS grid (real-app bug),
  now flexbox with drag handles + persisted widths.

### Canvas depth + test automation (2026-07-08, third pass)

- **Canvas multi-select** — shift-click, rubber-band, group move/delete/color,
  right-click **context menu** (duplicate / delete / add card). Was single-select.
- **Interactive table editor** — cell-precise click-to-edit + row/column controls.
- **Full-app e2e harness** (`src/testkit/tauriMock.ts` + Playwright): the whole
  `<App/>` runs against a mocked vault, so panes / sidebars / tabs / canvas are
  testable (they need the full app + Tauri). 6 e2e specs; CI runs them. This is
  what verifies the pane/sidebar features the editor harness can't reach.

- **Graph node colors by folder** (toggle + legend).
- **Bases editing**: added a **group-by** control (property + direction),
  interop-checked against the official syntax; buildViews now persists it.
  (A `sort` control was deliberately NOT shipped — Obsidian documents no
  per-view `sort` key, so writing one would corrupt the shared `.base`.)

- **Linked panes** — a 🔗 per-pane toggle; a linked pane follows notes opened
  in other panes.
- **Stacked tab groups** — a ▤ toggle spreads a pane's open tabs as read-only
  reading-view columns (the "see all open notes" overview).
- **Search `line:`** — `line:(a b)` matches terms co-occurring on one line.
- **Slides / presentation** — "Start presentation" splits on `---`, fullscreen
  with ←/→/Esc navigation.
- **Audio recorder** — "Record audio" captures from the mic, saves an
  attachment, and embeds it at the cursor.

### Final deep/deferred items (2026-07-08, closing pass)

- **Bookmarks create/edit** — toggle a file bookmark; the ONE `.obsidian/` write,
  done data-safely (opaque serde_json::Value preserves groups/nested/unknown
  fields; atomic write). An adversarial review then found + fixed 3 real bugs:
  group-nested toggle made a duplicate, heading/block (`subpath`) bookmarks were
  deleted by a file toggle, and a malformed file was silently discarded.
- **Deeper Bases editing** — a filter condition builder (multiple and/or string
  conditions), guarded so it only rewrites a filter the raw fully models (a
  parse-lossy tree stays read-only).
- **Inline-editable stacked columns** — stacked-tab columns are now live editors
  that save back per-note.

**The parity backlog is cleared.** The by-design exclusions remain out of scope
(Sync/Publish, mobile, community-plugin/theme marketplaces).

One capability gap is NOT excluded, just unbuilt: **Bases formula authoring**.
Formulas are already parsed, evaluated, usable as columns, and preserved on
save — the only missing piece is a UI to write/edit a formula's expression
(today done by editing the raw `.base` `formulas:` section). It's the next
tractable Bases increment; it needs an expression-language input (ideally with
validation) and `serializeBase` extended to write the document-level `formulas`
map.

## Scored matrix

| Dimension | ✅ Full | 🟡 Partial | ❌ Missing | ⭐ Bonus |
|---|--:|--:|--:|--:|
| Editor & Markdown | 11 | 6 | 2 | 0 |
| Links, Backlinks & Graph | 1 | 9 | 0 | 0 |
| Files & Vault | 5 | 6 | 0 | 0 |
| Panes & Workspace | 7 | 1 | 2 | 0 |
| Reading & Export | 1 | 2 | 0 | 1 |
| Canvas (JSON Canvas) | 2 | 3 | 0 | 0 |
| Properties & Bases | 1 | 3 | 0 | 0 |
| Core-plugin equivalents | 4 | 2 | 4 | 0 |
| Settings, Theming & Appearance | 2 | 4 | 1 | 0 |
| Obsidian interop & data safety | 3 | 2 | 0 | 0 |

## Prioritized gaps

| Sev | Effort | Status | Feature | Note |
|---|---|---|---|---|
| **high** | large | partial | CRLF / encoding preservation | Non-UTF8 encodings unsupported (read errors / lossy listing) and CRLF is stripped for any opened+edited note. Both a data-access and data-safety risk. |
| **high** | medium | partial | Rename with vault-wide link rewrite (canvas/base refs) | Rewrite loop touches only markdown notes; .canvas node paths and .base targets are left dangling on rename. Data-integrity deviation from Obsidian. |
| **high** | medium | partial | Plain-file format fidelity (CRLF + canvas geometry) | CodeMirror lacks a lineSeparator facet so first body edit normalizes CRLF->LF; canvas geometry rounded to ints on save. Silent file mutation. |
| **high** | small | partial | Internal markdown-link click-through in editor | Clicking [text](Note.md) in Live Preview is routed to the external URL opener (openUrl) and silently fails to navigate; only Reading View follows it. Real bug, cheap fix. |
| **medium** | large | partial | Settings UI structure + plugin/vault-config editability | Single-scroll modal vs tabbed panes; plugin API has no addSettingTab; vault settings (New links, Attachments, Daily notes) are display-only — src-tauri never writes .obsidian config. |
| **medium** | large | partial | Interactive table editor | Editing is raw-pipe markdown only; no in-place cell WYSIWYG, insert/delete row-column controls, alignment toggles, or Tab-between-cells. |
| **medium** | large | partial | Full-text search operators | Subset only (path/file/tag/exclude/phrase/regex + bare-term AND); missing OR/parentheses, line:/block:/section:, task:/task-todo:/task-done:, [key:value], match-case:, content:. |
| **medium** | large | partial | Global graph filters/colors/display/forces | Only global/local mode switch + pan/zoom; no search/tag/orphan filters, color groups, display settings (arrows/fade/node size/link thickness), or force sliders. |
| **medium** | large | partial | Canvas node types (file embed, create file/link/group, group containment) | File nodes show a filename/image card instead of embedded live note content; only text nodes are creatable (no drag-drop/menu for file/link/group); group nodes are inert (moving a group doesn't move contained nodes). |
| **medium** | large | partial | Bases editing (columns/types/formulas/filters/sort/group) | Display-name, property type/widget, formulas, and nested and/or/not filters are read-only; sort order, groupBy, per-column summaries, and cards image key round-trip via view.raw but aren't editable. |
| **medium** | large | missing | Vim key bindings | No Vim mode toggle in editor settings. |
| **medium** | medium | partial | Callout per-type icons + full type set | ~25 callout types collapse into 6 color groups (tip/success/check/hint/important all identical green) with NO per-type icons and no custom callout types. |
| **medium** | medium | partial | List / indentation folding | Only heading-section folding exists; nested list items cannot be collapsed (Obsidian's 'Fold indent'). |
| **medium** | medium | partial | Raw HTML in Live Preview | Block and inline HTML (<sup>/<mark>/etc.) show as literal text while editing; only Reading mode/export render it. Obsidian renders HTML inline in LP. |
| **medium** | medium | partial | Backlinks pane grouping/filter/sort/context | Renders an ungrouped, uncollapsible, unsortable, unfilterable flat list; no per-source headers, filter box, sort, or 'show more context'. |
| **medium** | medium | partial | Unlinked mentions Link/Link all + alias search | No per-mention 'Link' or 'Link all' action (only navigates), and only the note NAME is searched — aliases (aliasesOf exists) are not surfaced. |
| **medium** | medium | partial | Tag pane hierarchy + sort | Nested tags (#project/foo) shown as flat rows instead of a collapsible hierarchy with roll-up counts; sort fixed at count-desc (no frequency/name). |
| **medium** | medium | partial | File explorer sort/multi-select/collapse-all/folder-drag | Hard-codes folders-first + name-asc (no sort control), no multi-select, no collapse-all, and folders are not draggable. |
| **medium** | medium | partial | Bookmarks create/edit (write path) | Read-only: only readObsidianBookmarks exists, no write/add/toggle command. Can display/open externally-authored bookmarks but cannot create, star, group, reorder, or delete. |
| **medium** | medium | partial | Export to PDF (dedicated, with options) | PDF only via OS/webview print dialog's 'Save as PDF'; no direct export and none of Obsidian's options (page size, margins, orientation, downscale, title). |
| **medium** | medium | partial | Canvas node interactions (resize/multi-select/context menu) | Resize only from single bottom-right handle, no marquee/shift multi-select for group move/delete, and delete only via keyboard (no right-click menu). |
| **medium** | medium | partial | Canvas edge property editing | No UI to add/edit edge labels or colors, cannot reconnect/reroute endpoints, and new edges always get a single toEnd arrow with no direction/none toggle. |
| **medium** | medium | partial | Properties UI (rename key/reorder/autocomplete) | Existing property keys cannot be renamed (static div; input only for new props), no drag-reorder, and no name/value autocomplete or vault-wide suggestions. |
| **medium** | medium | missing | Note composer (merge/split/extract) | No 'merge with another file', 'split by headings', or 'extract selection/heading into new note' commands. |
| **medium** | medium | partial | Custom hotkeys — rebind built-in commands | Built-ins preventDefault first and the custom handler bails on defaultPrevented, so built-in chords can't be remapped; built-ins never appear in the Hotkeys manager and reassignment yields a silent dead key with no conflict warning. |
| **medium** | medium | missing | Font / appearance settings | No interface/text/monospace font-family or base font-size controls, and does not read appearance.json (baseFontSize/fonts/accentColor/cssTheme). Font size is an accessibility concern. |
| **medium** | medium | missing | RTL / bidirectional text support | No RTL toggle / auto-direction setting; CodeMirror renders bidi natively but Basalt exposes no explicit RTL feature (i18n/accessibility gap for RTL languages). |
| **medium** | small | partial | Wikilink [[ autocomplete create-new-note entry | Completion menu lists only existing notes/aliases; no 'create this note' option at the bottom for a non-existent name. |
| **medium** | small | partial | Templates date/time format settings | Reads only the templates 'folder' setting; {{date}}/{{time}} hardcoded to YYYY-MM-DD/HH:mm, so vaults with custom formats get different output than Obsidian. |
| **medium** | small | partial | Reading-mode task checkbox toggling | Checkboxes render as disabled inputs and ReadingView has no toggle handler; you cannot check off a task from Reading view as in Obsidian. |
| **medium** | small | partial | Quick switcher create-from-query + non-note files | Enter on a non-existent query does nothing (no 'Create note …'); only VaultNote items surface, no attachments/other files. |
| **medium** | small | partial | Hover page preview on markdown-style links | Preview scoped to [data-target] wikilinks only; .md-link/data-href internal links get no hover preview, and there is no modifier-key gating option. |
| **medium** | small | partial | Move note to vault root via drag | Drop targets are folder rows only; no root drop zone and no multi-file drag, so moving a note back to root requires the rename dialog. |
| **medium** | small | partial | Outgoing links pane clickable/unresolved + subpaths | Unresolved outgoing links are not clickable (no create/open), no per-item counts or grouping, and resolved links open at line 1 (no heading/block jump). |
| **medium** | small | partial | CSS snippets location + live reload | Reads .basalt/snippets not .obsidian/snippets (ignores existing vault snippets and enabledCssSnippets); no folder-watch, reload button, or open-folder button. |
| **low** | large | missing | Slides / presentation mode | No slide-deck / presentation mode. |
| **low** | medium | partial | Local graph depth/direction/filters | Depth fixed at 1 hop (localGraph accepts depth but UI never varies it); no direction control or the filters/display/forces panels. |
| **low** | medium | partial | Typed property Date-&-time widget + tag chips | date regex is date-only so datetime falls back to a plain text box (no datetime-local); type is inferred not user-selectable/persisted; 'tags' is a generic list, no chips/autocomplete. |
| **low** | medium | partial | Named workspaces (save/switch layouts) | Only one implicit auto-saved layout per window+vault; cannot name, save multiple, or switch profiles (Obsidian core Workspaces). |
| **low** | medium | missing | Linked panes (scroll/nav sync) | No 'Link with pane' to lock scroll/navigation between panes; each pane is independent. |
| **low** | medium | missing | Stacked tabs | No stacked-tabs display mode (overlapping vertical spines). |
| **low** | medium | missing | Audio recorder | No audio/video capture equivalent to Obsidian's Audio recorder plugin. |
| **low** | small | partial | Inline footnotes ^[...] in Live Preview | Inline footnotes not rendered as LP widgets (only reference-style [^id]); they render only in reading mode/export. |
| **low** | small | partial | %% comment concealment in Live Preview | %% comments are stripped from rendered output but not dimmed/hidden in LP; no editor extension touches %%. |
| **low** | small | missing | Random note | No open-a-random-note command. |
| **low** | small | partial | Ribbon extensibility/customization | Fixed hard-coded action set; plugin API has no addRibbonIcon and there is no reorder/hide/manage customization. |
| **low** | small | partial | Status bar content + extensibility | Character count hidden in a tooltip (not shown with word count), no backlink count/cursor position, and no addStatusBarItem for plugins. |

## Beyond-core (bonus)

- Export to HTML (Reading & Export) — a direct HTML export that Obsidian's core app does not provide out of the box

## Full results by dimension

### Editor & Markdown

| Feature | Status | Where / gap |
|---|---|---|
| Live Preview rendering | ✅ Full | src/editor/livePreview.ts:161-311 conceals HeaderMark/EmphasisMark/CodeMark(InlineCode)/StrikethroughMark/QuoteMark, renders ListMark bullets, TaskMarker checkboxes, HorizontalRule, Link/Autolink/URL, Image widgets; caret-touch reveals raw. Wired in renderExtensions (setup.ts:165-184).<br>**Gap:** Fenced code left raw (highlighted by codeBlocks.ts, matching Obsidian). Some inline constructs are handled by separate extensions (math/highlight/tags/footnotes), not this file. |
| Source mode toggle | ✅ Full | src/App.tsx:310 sourceMode state, :935 per-vault load from localStorage(`basalt.sourceMode.${root}`), :1564-1567 toggle+persist, :2929 command palette entry, :3178 toolbar button; src/editor/setup.ts:190-201 setSourceMode() swaps renderCompartment [] <-> renderExtensions(); wikilinkAutocomplete + wikilinkModClickFollow stay on in source mode (setup.ts:262-271, matching Obsidian). |
| Lists + interactive task checkboxes | ✅ Full | src/editor/livePreview.ts:93-125 CheckboxWidget click handler resolves live pos via posAtDOM and toggles [ ]<->[x]; :247-258 ListMark bullet widget / task-mark conceal; :260-265 TaskMarker checkbox render; list continuation via markdown() keymap (setup.ts:274).<br>**Gap:** Only standard GFM [ ]/[x] task states are recognized (regex /\[[ xX]\]/). Obsidian's extended states ([/], [-], [>], etc.) are not styled/toggled — but those are a theme/plugin feature, not core. |
| Tables render + edit | 🟡 Partial | src/editor/tables.ts:26-76 TableWidget renders GFM <table> as a block widget (StateField, tables.ts:117-133); :139-150 click maps caret into source; cursor-inside reveals raw markdown for editing.<br>**Gap:** Editing is raw-markdown only: cursor inside a table shows the pipe source. There is no Obsidian-style interactive table editor (in-place cell WYSIWYG editing, insert/delete row-column controls, alignment toggles, Tab-between-cells). |
| Callouts (all types) + fold +/- | 🟡 Partial | src/editor/callouts.ts:64 styling ViewPlugin, conceals [!type] on title line; calloutGroup() (callouts.ts:16-24) maps ~25 types into 6 color buckets; src/editor/calloutFold.ts:145 chevron fold with override StateField; reading-mode fold via <details> (render.ts:390-399).<br>**Gap:** ~25 callout types are collapsed into 6 color groups (tip/success/check/hint/important all render identical green), and there are NO per-type icons anywhere (grep of styles.css shows no callout icon glyphs). No custom-callout-type support. Obsidian gives each type a distinct icon + color. |
| Heading folding | ✅ Full | src/editor/headingFold.ts:27-47 headingSectionAt() walks ATXHeadingN siblings to the next same-or-higher heading; :74-96 custom gutter marks only heading lines with a fold click handler; :98 headingFold = [codeFolding(), headingFoldGutter]; placed outside renderCompartment (setup.ts:258) so it works in both modes. |
| Fold indentation / list folding | 🟡 Partial | src/editor/setup.ts:37-45 smartTab handles list indent (LIST_LINE regex) + indentMore; headingFold.ts:98 uses codeFolding() but the custom gutter (headingFold.ts:78-82) returns a marker ONLY for heading lines. @codemirror/lang-markdown defines no foldNodeProp for lists, so foldKeymap finds nothing foldable on a list.<br>**Gap:** No collapsible list/indent folding (Obsidian's 'Fold indent'). Only heading-section folding is implemented; nested list items cannot be collapsed. |
| Math (KaTeX) inline + block | ✅ Full | src/editor/math.ts:18 MATH_RE handles $$...$$ (display, single-line-alone or multi-line block) and $...$ (inline, no leading/trailing space); :48-70 MathWidget lazy-loads ../lib/math (KaTeX); :77 isInExcludedRegion skips math inside code; caret-inside reveals raw.<br>**Gap:** Only $/$$ delimiters (Obsidian uses the same); no \(...\)/\[...\] TeX delimiters, but that is not Obsidian behavior either. |
| Mermaid diagrams | ✅ Full | src/editor/mermaid.ts:40-58 StateField iterates FencedCode nodes, checks CodeInfo lang === 'mermaid', renders CodeText via lazy renderMermaid; :29-31 error boundary sets .cm-mermaid-error; :68-78 click places caret to edit. |
| Footnotes | 🟡 Partial | src/editor/footnotes.ts:15 REF_RE renders [^id] references as superscript widgets in Live Preview, explicitly EXCLUDING inline ^[...] (footnotes.ts:14-15 comment + REF only matches [^id]); definitions [^id]: skipped (footnotes.ts:52-55). src/lib/render.ts:75-95,168-171 renders BOTH inline ^[...] (m[13]) and [^id] (m[14]) + backlinked footnote section in reading/export.<br>**Gap:** Inline footnotes ^[...] are NOT rendered as widgets in the editor Live Preview (only reference-style [^id] is). They render only in reading mode/export. |
| Highlight == and comments %% | 🟡 Partial | src/editor/highlight.ts:14-40 ==highlight== concealed marks + cm-highlight class in Live Preview. For %%: grep of src/editor/ returns ZERO matches; %% is handled only in src/lib/render.ts:249-251 stripComments() (reading/export).<br>**Gap:** %% comments are stripped from rendered output but NOT concealed/hidden in the Live Preview editor (Obsidian dims/hides them in LP). No editor extension touches %%. |
| Tags #tag | ✅ Full | src/editor/tags.ts:11-32 tagRegex() marks #tag/#nested/tag with cm-tag chip class; skips pure-numeric (Obsidian rule), frontmatter YAML tags: lines, code/table regions (isInExcludedRegion), and link contexts (isInLinkContext).<br>**Gap:** Tags are style-only marks (no click-to-search in the editor yet, per the file's own comment), but tag recognition/rendering is complete. |
| Block refs ^id and heading # link targets | ✅ Full | src/editor/wikilink.ts:116-158 completion: [[Note#^ suggests block ids via getBlockIds (id+snippet), [[Note# suggests headings via getHeadings; apply absorbs existing ]] closer; onOpen passes the full target incl. #heading/#^block (wikilink.ts:214-216, 246).<br>**Gap:** Whether opening [[Note#heading]] scrolls to the anchor is app-level navigation (not verified in editor scope); the editor correctly captures and forwards the anchored target. |
| Transclusion / embeds ![[Note#h]] / #^block | ✅ Full | src/editor/embeds.ts:12-50 image embeds ![[img.png]] via ImgWidget (with \|width parsing); src/editor/transcludeBlocks.ts:70-98 StateField renders note/heading/block transclusions (TranscludeWidget via renderEmbedSource) and audio/video/pdf (MediaWidget via buildMediaElement); caret-inside reveals raw. |
| Raw HTML in markdown | 🟡 Partial | src/lib/render.ts:193-338 HTML_BLOCK detects block HTML -> data-basalt-html placeholder; inline safe tags via m[15] (render.ts:172-174); src/lib/sanitize.ts DOMPurify. BUT grep shows data-basalt-html/raw-html only in src/lib (reading mode + export) — NOT in src/editor/. livePreview.ts has no HTML handling.<br>**Gap:** Raw HTML renders only in Reading mode and export, NOT in the editor Live Preview (block HTML and inline <sup>/<mark>/etc. show as literal text while editing). Obsidian renders HTML inline in Live Preview. |
| Editing keys (Mod-B/I/K, Tab indent, list continuation, auto-pair, multi-cursor) | ✅ Full | src/editor/markdownKeys.ts:101-105 Mod-b/i/k with syntax-tree-based toggle/unwrap (enclosing()); setup.ts:38-46 smartTab (indent lists, literal tab elsewhere), :239 closeBrackets, :242 allowMultipleSelections, :245 wrapSelectionOnType, :279 Tab/shift-indentLess, :282 searchKeymap (Mod-D select-next), :236 rectangularSelection (Alt-drag), list continuation via markdown() keymap (:274).<br>**Gap:** Mod-K inserts a standard [](url) link (not a wikilink), matching Obsidian's default. |
| Spellcheck | ✅ Full | src/App.tsx:326-329 spellcheck state + localStorage('basalt-spellcheck') persistence, :2959 toggle command; src/editor/setup.ts:139-148 spellcheckCompartment reconfigures EditorView.contentAttributes({spellcheck}); initial value threaded via createEditorState (setup.ts:250-252). |
| Vim key bindings | ❌ Missing | grep -rni 'vim' src/ returns ZERO matches; no @replit/codemirror-vim or @codemirror/vim in package.json dependencies.<br>**Gap:** No Vim mode. Obsidian offers a Vim keybindings toggle in Editor settings. |
| RTL / bidirectional text | ❌ Missing | grep for rtl/bidi/direction: the only `direction: rtl` is styles.css:1481, used for file-tree path truncation (keep the tail visible) — not the editor. No bidi library in deps; no per-line direction or RTL toggle in src/editor/.<br>**Gap:** No RTL editing support / auto-direction / RTL toggle (Obsidian has an 'RTL' editor setting). CodeMirror renders bidi text natively at the browser level, but Basalt exposes no explicit RTL feature. |

### Links, Backlinks & Graph

| Feature | Status | Where / gap |
|---|---|---|
| Wikilinks [[ ]] click/create + [[ autocomplete | 🟡 Partial | src/editor/wikilink.ts:78-107 (widget decoration), 211-221 (widget click → onOpen), 231-254 (mod-click follow in source mode), 109-158 (heading `#` + block `#^` completion), 160-191 (note/alias completion). App.tsx:1839-1876 handleOpenWikilink resolves; on miss calls createAndOpen (create-on-click works for editor widgets AND ReadingView). getNotes (App.tsx:1518-1523) feeds real notes + allAliases into completion.<br>**Gap:** The `[[` completion menu offers ONLY existing notes/aliases (wikilink.ts:160 `opts.getNotes()`); it has no "create new note" entry. Obsidian's [[ completion always shows a create-this-note option at the bottom when the typed name doesn't exist. Creation in Basalt only happens by clicking/following a fully-typed unresolved link, not from the completion list. |
| Markdown-style [text](note.md) internal links | 🟡 Partial | Resolve: src/lib/markdown.ts:116-150 + vaultIndex.ts:110-124 (extractLinks scans md links) + resolve() handles them. Rename-rewrite: src/lib/rename.ts:22-62 (rewriteMdLine) + folderMoveMapper. ReadingView.tsx:145-154 correctly splits internal (internalMdHref) vs external. BUT src/editor/livePreview.ts:333-343 (linkClick) routes EVERY .cm-md-link click to opts.onOpenUrl(dataset.href), and App.tsx:1726-1730 handleOpenUrl calls Tauri openUrl().<br>**Gap:** In the editor / live-preview, clicking an internal markdown link [text](Note.md) is handed to the EXTERNAL url opener (openUrl("Note.md")), which silently fails — it does not navigate to the note. Internal md-link click-through works only in Reading View. Obsidian follows internal markdown links in both edit and reading modes. |
| Aliases (frontmatter aliases:) | ✅ Full | src/lib/vaultIndex.ts:178-218 (frontmatterAliases, quote-aware split), 278/374-386 (separate byAlias map so real files win), 480-483 (alias resolution only for bare names, after real files — correct Obsidian precedence), 321-330 (allAliases for completion), App.tsx:1523 injects aliases into getNotes with alias round-trip guard (wikilink.ts:171-179 inserts alias text verbatim).<br>**Gap:** Alias precedence, resolution, backlink-via-alias (backlinksFor uses resolve()), and completion all present. Minor: unlinked-mention scanning is NOT run over aliases (see Unlinked mentions feature), and aliases containing #\|[] are excluded from completion (a deliberate, correct round-trip guard). |
| Backlinks pane (linked mentions) | 🟡 Partial | src/components/Backlinks.tsx:39-57 + RefList 10-35 renders a FLAT list (one button per occurrence, note name repeated each row). src/lib/vaultIndex.ts:568-581 backlinksFor() correctly resolves incoming links (skips self, uses full resolve() so folder/alias/relative forms count).<br>**Gap:** Obsidian's backlinks pane groups mentions under a collapsible per-source-file header, and offers an in-pane filter/search box, sort options (by name/modified time), and "show more context" line expansion. Basalt renders an ungrouped, uncollapsible, unsortable, unfilterable flat list. |
| Unlinked mentions | 🟡 Partial | src/components/Backlinks.tsx:49-54 renders unlinked via the same read-only RefList (open-on-click only). src/lib/vaultIndex.ts:588-620 unlinkedMentionsFor() finds bare-name mentions, masks inline code + wikilinks + md-links, skips frontmatter/fenced code (proseMask). App.tsx:2294-2296 calls it with activeName ONLY.<br>**Gap:** Two Obsidian sub-features missing: (1) no per-mention "Link" button and no "Link all" action to convert an unlinked mention into a real link — Basalt only navigates to the line. (2) Only the note's NAME is searched (App.tsx:2296 passes activeName); Obsidian also surfaces unlinked mentions of the note's ALIASES. aliasesOf() exists but isn't passed here. |
| Outgoing links pane | 🟡 Partial | src/components/RightPanel.tsx:82-101 (Links tab). Resolved links render as clickable buttons (line 87-91 → onOpenRef(l.path,1)); unresolved render as plain non-interactive <div className="outgoing-item unresolved"> (line 95-99). src/lib/vaultIndex.ts:543-565 outgoingLinksFor() dedupes resolved + unresolved.<br>**Gap:** Unresolved outgoing links are NOT clickable — Obsidian lets you click an unresolved outgoing link to create/open the note. Also no per-item counts and no grouping. Resolved links open at line 1 only (no heading/block subpath jump). |
| Global graph view | 🟡 Partial | src/components/GraphView.tsx:32-344 (D3 force sim, canvas render, pan/zoom, drag, hover-neighborhood highlight, label LOD, position/viewport cache). Header (315-338) exposes ONLY Global/Local buttons, a node/link count, and close. src/lib/vaultIndex.ts:487-503 graph().<br>**Gap:** Confirmed: no filters (search, tag/attachment/orphan toggles, existing-files-only), no color groups, no display settings (arrows, text-fade, node size, link thickness, animate), no force sliders (center/repel/link/distance). Only global/local mode switch + pan/zoom. |
| Local graph view | 🟡 Partial | src/components/GraphView.tsx:21-30 (mode prop), 318-331 (Global/Local buttons). src/lib/vaultIndex.ts:506-539 localGraph(centerPath, depth=1) does support a configurable undirected-neighbor depth. BUT App.tsx:2340-2346 calls index.current.localGraph(graphKey, 1) — depth is HARDCODED to 1 with no UI control.<br>**Gap:** Depth is fixed at 1 hop; Obsidian's local graph has a depth slider (1..N), plus a links-direction control (incoming/outgoing/both) and the same filters/display/forces panels as the global graph — none exposed. localGraph() accepts depth but the UI never varies it. |
| Hover page preview | 🟡 Partial | src/lib/hoverPreview.ts:1-116 — delegated mouseover on [data-target], show/hide delay timers, edge-flip position(), renders target via renderMarkdown with lazy image/math/HTML fill. Fires on wikilink widgets (WikilinkWidget sets dataset.target, wikilink.ts:68) and ReadingView wikilinks.<br>**Gap:** Preview is scoped to [data-target] wikilinks only. Markdown-style internal links render as .md-link with data-href (render.ts:147, livePreview.ts:150-151) and get NO hover preview. Obsidian's page preview covers ALL internal links (including [text](note.md)). Also no modifier-key gating option (Obsidian lets you require Cmd/Ctrl-hover). |
| Tag pane | 🟡 Partial | src/components/Tags.tsx:9-26 renders a FLAT list of clickable rows (#tag + count → onSelect searches). src/lib/vaultIndex.ts:359-372 allTags() aggregates counts lowercased, sorts by count desc then name. extractTags (246-268) reads frontmatter tags: + inline #tags, skipping code/frontmatter.<br>**Gap:** Nested tags like #project/foo are shown as individual flat rows; Obsidian's tag pane is a COLLAPSIBLE HIERARCHY that nests #project/foo under a #project parent with roll-up counts. Also no sort control (Obsidian offers frequency / name A-Z / Z-A); Basalt's sort is fixed at count-desc. |

### Files & Vault

| Feature | Status | Where / gap |
|---|---|---|
| File explorer tree | 🟡 Partial | src/components/Sidebar.tsx:50-115 (buildTree, expand/collapse, persisted expansion, auto-reveal ancestors of activePath), src/lib/tree.ts (buildTree/ancestorFolders). Media/attachment files shown and dimmed.<br>**Gap:** vs Obsidian file explorer: no sort-order control (Obsidian offers name A-Z/Z-A, modified time, created time — Basalt hard-codes folders-first + name-asc in tree.ts:20-28); no multi-select; no 'collapse all' button; folders are not draggable (Sidebar.tsx:202 sets draggable only on non-attachment FILES). |
| New note / new folder (in folder) | ✅ Full | src/App.tsx:2233-2249 (handleNewNoteIn uniquifies 'Untitled'/'Untitled N' within folderRel, then createAndOpen), App.tsx:3502-3521 (folder context menu 'New note here' + 'New folder…' → setSubfolderParent), App.tsx:2958 ('new-folder' command creates at vault root via setSubfolderParent('')). createAndOpen at App.tsx:1819 opens the created note.<br>**Gap:** (minor) Obsidian drops a newly created note straight into inline rename in the tree; Basalt creates 'Untitled' and opens it — renaming is a separate dialog/F2 step. |
| Rename note with vault-wide link rewrite | 🟡 Partial | src/App.tsx:2452-2619 (handleRenameNote: flush, pre-rename resolver snapshot, fs rename, re-anchor own self/relative links, rewrite every affected source READ FROM DISK), src/lib/rename.ts:72-130 (rewriteLinks handles wikilinks + markdown links, preserves #heading/^block + \|alias, respects shortest/relative/absolute newLinkFormat). Genuinely thorough for markdown links.<br>**Gap:** Not truly 'vault-wide': the rewrite loop iterates only preNotes (markdown notes). src/lib/canvas.ts:27-28 shows .canvas file-nodes reference notes by vault-relative path, and handleRenameNote never touches .canvas or .base files — so a renamed note's references inside a Canvas (or Bases file targeting it) are left dangling, whereas Obsidian updates canvas node paths on rename. |
| Move note (drag-drop / to folder) | 🟡 Partial | src/components/Sidebar.tsx:180-191 (onDragOver/onDrop on FOLDER rows only → onMoveToFolder), App.tsx:2899-2908 (handleMoveToFolder delegates to handleRenameNote so links are rewritten). Drag-onto-folder with link rewrite works correctly.<br>**Gap:** Drag targets only folder rows: the .note-list container (Sidebar.tsx:133) has no root drop zone, so a note cannot be dragged back to the vault root (Obsidian allows dropping on the vault-name/empty area); no multi-file selection/drag. Moving to root is only possible via the rename dialog editing the path. |
| Rename / move / delete FOLDER | ✅ Full | src/App.tsx:2682-2895 (handleRenameFolder: single fs::rename of the whole subtree via renameFolder, then one vault-wide pass rewriting markdown links AND attachment embeds whose resolution changed, with case-only-rename and move-into-self guards), App.tsx:2623-2675 (handleDeleteFolder → .trash with flush/conflict discipline), App.tsx:3512-3541 (folder context menu Rename/New folder/Delete), src-tauri/src/lib.rs:547-587 (delete_folder to .trash, dot-folder/symlink guards, timestamp collision).<br>**Gap:** Move is via a path-edit prompt rather than drag (folders aren't draggable). Shares the canvas/base limitation: .canvas/.base references to moved notes are not updated. No 'Search in folder' / bookmark-folder context actions Obsidian offers. |
| Delete to .trash (recoverable) | ✅ Full | src-tauri/src/lib.rs:515-541 (delete_note: fs::rename into <vault>/.trash, timestamp suffix on collision), lib.rs:547-587 (delete_folder same), src/lib/vault.ts:83-86 (deleteNote wrapper), src/App.tsx:2367-2398 (handleDeleteNote confirms, cancels autosave, clears snapshots, prunes tabs).<br>**Gap:** Obsidian exposes a 'Deleted files' setting with three modes (system trash / .trash folder / permanently delete); Basalt hard-codes the .trash-folder behavior with no configuration. |
| Attachments: images, PDF, audio, video | ✅ Full | src-tauri/src/lib.rs:850-853 (ATTACHMENT_EXTS incl. png/jpg/jpeg/gif/svg/webp/bmp/avif/ico/pdf/mp3/wav/m4a/ogg/flac/mp4/mov/webm), src/editor/attachments.ts:54-98 (paste/drop filter + placeholder→![[name]] swap), src/lib/attachments.ts:9-26 (resolveAttachment). RENDERING verified: images in Live Preview (src/editor/embeds.ts), audio/video/PDF players in Reading view (src/lib/render.ts:127 + ReadingView.tsx:68-70 fillMedia) AND Live Preview (src/editor/transcludeBlocks.ts:85-91 MediaWidget via src/lib/media.ts buildMediaElement).<br>**Gap:** (none material) PDF uses <embed> (native viewer) rather than a scroll-to-page embed; audio/video are standard HTML players. |
| Bookmarks / starred | 🟡 Partial | src/components/Bookmarks.tsx (renders Bookmark[] of type file/folder/heading/block/search/graph with group headers, opens files/routes search bookmarks), src/lib/vault.ts:181-193 (readObsidianBookmarks from .obsidian/bookmarks.json), App.tsx:928 (setBookmarks(await readObsidianBookmarks())).<br>**Gap:** READ-ONLY: there is no write path anywhere — grep found only readObsidianBookmarks, no writeBookmark/addBookmark/toggleBookmark/write_bookmarks command. You cannot create, star, edit, reorder, group, or delete a bookmark from within Basalt; it can only display and open bookmarks authored externally by Obsidian. |
| File recovery / version history | ✅ Full | src/components/VersionHistory.tsx (browse snapshots newest-first, preview, 'Restore this version'), src/lib/snapshots.ts:14-45 (2-min interval, MAX_SNAPSHOTS 100/note, 30-day retention, dedup, prune), snapshots.ts:132-189 (IndexedDB record-per-note, force-capture before restore, rename/clear migration), App.tsx:3412-3419 wires it.<br>**Gap:** No diff view — Obsidian's File Recovery highlights changes between the snapshot and current content; Basalt shows a plain <pre> of the snapshot (VersionHistory.tsx:54). Interval/retention are hard-coded (not user-configurable as in Obsidian settings). |
| Quick switcher (open by name, MRU) | 🟡 Partial | src/App.tsx:2911-2923 (switcherItems: MRU order from recents on blank query, fuzzyRank on typed query), App.tsx:3269-3287 (switcher modal), src/components/Palette.tsx:55-59 (Enter selects items[active] only — no create branch).<br>**Gap:** Confirmed no create-from-query: Palette's Enter handler only opens an existing item, so pressing Enter on non-existent query text does nothing (Obsidian offers 'Create note …'). Also surfaces only VaultNote items — no attachments/other files. |
| Full-text search + operators | 🟡 Partial | src/lib/search.ts:60-84 (parseSearchQuery: path:/file:/tag:/-exclude/"phrase"/ /regex/ + AND-ed bare terms), search.ts:98-150 (searchVault ranks title-first then line hits, NFC/case-insensitive, caps), App.tsx:3288-3309 (search modal).<br>**Gap:** Operator set is a subset of Obsidian's. Missing: OR and parenthetical grouping; line:/block:/section: scoping; task:/task-todo:/task-done:; property search [key:value]; match-case:; content:. Basalt only ANDs bare terms and supports path/file/tag/exclude/phrase/regex. |

### Panes & Workspace

| Feature | Status | Where / gap |
|---|---|---|
| Tabs | ✅ Full | src/components/TabBar.tsx: TabItem {path,name,pinned} (lines 3-7); full tab bar renders multiple tabs per pane (line 61 map), new-tab button (line 124), close button (108-120), middle-click close (80-83). Wired in src/App.tsx renderPane (lines 3042-3055) with per-pane tab arrays; openInPane always adds each note as its own tab (line 800 area).<br>**Gap:** Right-click context menu ONLY toggles pin (TabBar.tsx:88-91) — no Obsidian-style 'Close others / Close all / Move to new window' menu. The '+' button opens the quick-switcher modal (App.tsx:3051-3054) rather than an empty new-tab page. No tab-overflow dropdown when tabs exceed width. Core 'multiple note tabs' is fully met. |
| Split panes (horiz/vert) | ✅ Full | src/lib/workspace.ts: LayoutNode split {dir:'row'\|'col',sizes,children} (lines 9-11), splitLeaf with same-direction flattening (lines 32-59), removeLeaf collapse (66-74), setSizes (77-88). src/components/PaneTree.tsx renders recursive splits + draggable Resizer (lines 16-64). App.tsx splitFocused opens the same note in the new pane matching Obsidian (835-860); commands split-right/split-down (2960-2961) + Ribbon button (3194).<br>**Gap:** Only 'split right' (row) and 'split down' (col) are exposed; no drag-tab-to-pane-edge to CREATE a split — handleTabDrop (App.tsx:810-831) only moves tabs between ALREADY-EXISTING panes, whereas Obsidian creates a new split when a tab is dropped on a pane edge. |
| Tab pinning | ✅ Full | src/components/TabBar.tsx: pinned field (line 6), onTogglePin (18), right-click toggle (88-91), pin glyph 📌 with unpin button (93-105), and pinned tabs render NO close button (108) + middle-click guarded (83). Persisted: pane.pinned string[] saved in SavedWorkspace (App.tsx:1005) and restored (898-899). handleTabDrop unpins on cross-pane move (821-824).<br>**Gap:** Obsidian's pin also prevents a tab from being REPLACED on navigation; in Basalt every note always opens as its own tab (openInPane never replaces), so that semantic is moot — pin here only prevents closing. Behavioral simplification, not a functional loss. |
| Drag tabs to reorder / between panes | ✅ Full | src/components/TabBar.tsx: TAB_MIME 'application/x-basalt-tab' drag payload '<paneId>\n<path>' (lines 23-24,69-71), drop-indicator positioning by cursor half (73-79), bar-level drop (41-59). src/lib/tabs.ts: reorderTabs (5-14) + insertTab (17-22). App.tsx handleTabDrop handles same-pane reorder AND cross-pane move via openInPane+insertTab+closeTab (810-831).<br>**Gap:** Cannot drag a tab OUT to create a new split (edge drop) or drag a tab into a separate OS window — only reorder within a pane and move between existing panes. |
| Linked panes (scroll/nav sync) | ❌ Missing | grep for 'linked/sync.*pane/lock.*pane/scroll.*sync' across src/ returns only unrelated hits (outgoing-links pane, unlinked mentions, backlinks). No leaf-group / linked-scroll / linked-navigation state anywhere in workspace.ts or App.tsx.<br>**Gap:** No mechanism to lock scroll position or navigation between two panes (Obsidian's 'Link with pane' / linked group). Each pane is fully independent. |
| Stacked tabs | ❌ Missing | grep 'stack' across src/ finds only algorithmic stacks (bases.ts parser, render.ts list stack, folderMove) — nothing pane/tab related. TabBar.tsx renders tabs as a linear flex row only; no stacked/accordion mode toggle in App.tsx commands (2946-2996).<br>**Gap:** No stacked-tabs display mode where tabs fan out as overlapping vertical spines. |
| Named workspaces (save/load layouts) | 🟡 Partial | src/App.tsx: SavedWorkspace interface {layout,panes,focusedId} (178-182) auto-persisted to a single per-window-per-vault key workspaceKey=wsKey(vault,WINDOW_LABEL) (line 175) via effect (1001-1013), auto-restored on openVault (925,968). No command to create/name/save/list/switch multiple workspace profiles anywhere in the command registry (2946-2996).<br>**Gap:** Only ONE implicit auto-saved layout per window+vault. No equivalent of Obsidian's core Workspaces plugin: cannot name, save multiple, or switch between named layout profiles. |
| Sidebar toggles (left/right collapse) | ✅ Full | src/App.tsx: leftOpen/rightOpen state (287-288); commands toggle-left-sidebar (⌘\) + toggle-right-sidebar (⌘⌥\) (2946-2947); keyboard shortcut with alt for right (1309-1310); Ribbon UI button for left (3136); conditional render (3146,3246).<br>**Gap:** Collapse state is NOT persisted — leftOpen/rightOpen initialize to useState(true) with no localStorage read, so both sidebars always reopen on app restart (Obsidian remembers collapsed state). Also the Ribbon exposes a UI button only for the LEFT sidebar; the right sidebar is toggle-able via command/keyboard only. |
| Multi-window (open in new window) | ✅ Full | src-tauri/src/lib.rs open_new_window (298-321) builds a real WebviewWindow with a unique reused 'w<n>' label + own VaultState, passing vault via ?vault= URL param. App.tsx handleOpenInNewWindow (1356), 'New window' command (2953), URL-driven restore in launch effect (985-994) so a secondary window reloads its own vault.<br>**Gap:** A new window can only open a VAULT (picker or ?vault=path); there is no 'move THIS tab/note to a new window' — Obsidian can pop a specific note out into its own window. handleOpenInNewWindow takes a vaultPath, not a note path. |
| Multi-vault switcher | ✅ Full | src/components/VaultSwitcher.tsx: recents list with relative-time (21-30,80-107), current-vault badge (94), open-here (onOpen) vs open-in-new-window (onOpenNewWindow, ⧉ button 95-104), 'Open folder…' + 'New window' actions (109-116), keyboard nav ↑/↓/Enter and ⌘/Ctrl-Enter for new window (50-66). Wired via App.tsx modal 'switcher'/'vaults' + recentVaults.ts persistence.<br>**Gap:** (none material) — modal-based rather than Obsidian's separate vault-manager window, but functionally covers open/switch/recents. |

### Reading & Export

| Feature | Status | Where / gap |
|---|---|---|
| Reading mode (rendered read-only) | 🟡 Partial | src/components/ReadingView.tsx:26-135 renders a full rendered view (markdown, dataview/query blocks, transclusion embeds, math, raw HTML sanitized via DOMPurify, media players, plugin code-block processors, mermaid→SVG, resolved vault images). Wired at src/App.tsx:3087-3098; state at App.tsx:311-312. Comprehensive rendering pipeline.<br>**Gap:** Task checkboxes are rendered non-interactive: render.ts:236 emits `<input type="checkbox" disabled>`, and ReadingView's onClick (ReadingView.tsx:137-155) only handles .md-wikilink/.md-link — there is no checkbox-toggle handler. Obsidian's Reading view lets you click a task checkbox to toggle it in the source note; Basalt cannot. Narrow but real deviation. |
| Export to PDF | 🟡 Partial | src/App.tsx:1655-1665 handlePrintPdf switches to Reading view then calls window.print(); command 'Print / Save as PDF…' at App.tsx:2932. No PDF library in package.json, no Tauri PDF plugin / printToPDF in src-tauri (grep for pdf/print/wry/webkit in Cargo.toml empty; no jspdf/puppeteer/html2canvas).<br>**Gap:** No programmatic/dedicated PDF export. PDF is only reachable via the OS/webview print dialog's 'Save as PDF'. Obsidian's core 'Export to PDF' generates a PDF directly and offers options (page size, margins, portrait/landscape, downscale, include title) — none of which exist here. |
| Export to HTML | ⭐ Bonus | src/App.tsx:1583-1651 handleExportHtml builds a self-contained HTML file (buildHtmlDocument in src/lib/export.ts:44-59, tested in export.test.ts:4-16). Inlines images as data URLs, renders math→MathML, mermaid→SVG, sanitizes raw HTML; saves via Tauri dialog + exportFile.<br>**Gap:** (1) Obsidian core has NO 'Export to HTML' — it ships only 'Export to PDF'; HTML export is a community-plugin capability. So this is a bonus, not a parity item. (2) The export is also incomplete vs Basalt's own Reading view: App.tsx:1595-1639 only processes [data-math], [data-basalt-html], mermaid, and images. It does NOT run renderQuerySource (dataview/query blocks), renderEmbedSource (![[ ]] transclusions), fillMedia (audio/video/pdf), or codeBlockProcessor (plugin blocks) — so those export as raw <pre> code or unresolved markers. |
| Print | ✅ Full | src/App.tsx:1655-1665 handlePrintPdf forces Reading view + window.print(); print styling at src/styles.css:663-675 (@media print hides sidebar/toolbar/tab-bar/panels, expands .reading-view, forces legible light color). Command registered at App.tsx:2932.<br>**Gap:** None material for print itself. Note it is the exact same handler as the PDF path (handlePrintPdf) and, like Obsidian, there is no separately configurable print-options dialog beyond the OS/webview one. Obsidian has no dedicated 'Print' command either. |

### Canvas (JSON Canvas)

| Feature | Status | Where / gap |
|---|---|---|
| Open .canvas (pan/zoom) | ✅ Full | /Users/oweneldridge/projects/basalt/src/components/CanvasView.tsx:156-206 (pan via pointer drag on empty space, zoom via wheel toward cursor), :124-144 (fit-to-view on external load only, preserving user pan/zoom on edits); parseCanvas tolerant load /Users/oweneldridge/projects/basalt/src/lib/canvas.ts:134-227<br>**Gap:** Behavioral deviation from Obsidian: the wheel ALWAYS zooms (line 185-195), whereas Obsidian scroll-pans and reserves zoom for pinch/cmd+scroll; two-finger trackpad scroll therefore zooms instead of panning. No zoom-control buttons (zoom in/out/fit/zoom-to-selection) that Obsidian provides. Pan still reachable via empty-space drag, so both actions work. |
| Create/move/resize/delete nodes | 🟡 Partial | Create: double-click empty space + '+ Card' button both create TEXT nodes only (CanvasView.tsx:334-352, :367-388). Move: startNodeDrag :235-265. Resize: startResize :267-297. Delete: Delete/Backspace keyhandler :208-231.<br>**Gap:** Real shortfalls vs Obsidian: (1) resize is only from a SINGLE bottom-right handle (:568, startResize grows w/h from that corner) — Obsidian resizes from any edge/corner. (2) NO multi-select — `selected` is a single string (:86); you cannot marquee/shift-select to move or delete multiple nodes together, a core Obsidian canvas interaction. (3) Delete only via keyboard, no right-click context menu. (4) Only text nodes are creatable (see node-types feature). |
| Node types: text, file, link, group | 🟡 Partial | All four parse (canvas.ts:172-179) and render (CanvasView.tsx:572-650): group as bordered/labeled box, file as image or 📄 filename card, link as 🔗 url card, text as rendered markdown.<br>**Gap:** Significant display/authoring deviations: (1) FILE nodes render only as a filename card (:589-602) or image — Obsidian embeds the actual note's live content inline; subpath is parsed but never shown. (2) Only TEXT nodes can be CREATED via UI — no drag-drop, no menu to add file/link/group nodes (grep confirms no onDrop/DataTransfer, no type:'file'/'link'/'group' creation). (3) GROUP nodes are inert boxes: moving a group does NOT move contained nodes (no containment logic), unlike Obsidian groups. |
| Edges between nodes | 🟡 Partial | Create by dragging side anchor onto a target node (CanvasView.tsx:299-332, auto-picks toSide, defaults toEnd:'arrow'). Render as bezier with arrow markers (:413-444), labels (:437-441) and edge color (:432) drawn from parsed data. Delete via selecting edge + Delete (:224-226). Parser validates/round-trips fromSide/toSide/fromEnd/toEnd/color/label (canvas.ts:207-218).<br>**Gap:** Edge EDITING is largely absent vs Obsidian: (1) no UI to add/edit an edge label (labels only render from file — grep found no label input/prompt). (2) no UI to set edge color. (3) cannot reconnect/re-route an existing edge's endpoints by dragging them. (4) new edges always get a single toEnd arrow; no way to toggle arrowhead direction or make it none. So creation+display+delete work but property editing does not. |
| Node colors | ✅ Full | canvasColor maps presets 1-6 (canvas.ts:248-264) with prototype-safe guard; hex passes through. Toolbar shows 6 preset dots + 'No color' for the selected node and writes via patchNode (CanvasView.tsx:389-400); applied to node/group border (:518-524); clearing color drops the key on serialize (canvas.ts:77-89).<br>**Gap:** Two deviations from Obsidian's color menu: (1) UI offers only the 6 presets + none — no CUSTOM hex color picker (hex round-trips from file but cannot be authored in-app, COLORS array is [undefined,'1'..'6']). (2) color tints only the BORDER; Obsidian also applies a subtle background tint to the node. |

### Properties & Bases

| Feature | Status | Where / gap |
|---|---|---|
| Properties (frontmatter) UI editor | 🟡 Partial | src/editor/frontmatter.ts:PropertiesWidget — live-wired via src/editor/setup.ts:167. Value editing, +Add property (appendNewRow 251-305), delete (204), and 'Edit as text' (89-95) all work; lib/frontmatter.ts setProp/deleteProp do a faithful span-only round-trip (proven by frontmatter.test.ts).<br>**Gap:** Falls short of Obsidian's Properties editor: (1) an EXISTING property key cannot be renamed — the key is a static <div> (frontmatter.ts:105-108); a key <input> exists only for brand-new props (appendNewRow). (2) No drag/reorder of properties (Obsidian supports it). (3) No name/value autocomplete or vault-wide suggestions (no datalist/suggest anywhere in the widget). |
| Typed property widgets (text/number/date/checkbox/list/tags) | 🟡 Partial | src/lib/frontmatter.ts:scalarType (32-40) infers boolean/number/date/text; src/editor/frontmatter.ts renders checkbox (138-146), typed number/date inputs guarded by isValidNumber/isValidDate (147-182), list/inline pill inputs (120-137), text fallback (183-195). frontmatter.test.ts validates type inference and raw/quoted round-trip.<br>**Gap:** No Date-&-time widget: scalarType's date regex is date-only (^\d{4}-\d{2}-\d{2}$), so a datetime value classifies as 'text' and gets a plain text box — there is no <input type=datetime-local> (Obsidian has a first-class Date & time type/picker). Type is inferred from value shape only; it is not user-selectable or persisted per-property as in Obsidian. 'tags' is not a distinct widget — rendered as a generic list, no tag chips/autocomplete. |
| Bases database views (read) | ✅ Full | src/lib/bases.ts: parseBase, runView (1713-1853) implement filters (and/or/not, evalFilter 1611-1618), multi-key sort (1753-1776), groupBy (1793-1811), limit, builtin + custom summaries (1670-1841), formulas, and a full expression language (string/number/list/date/file/link methods, ReDoS + step/alloc/deadline budgets). src/components/BaseView.tsx renders table (with groups + summary tfoot) and cards.<br>**Gap:** Minor render-side deviations from Obsidian: Cards view hardcodes the first 4 properties (BaseView.tsx:503 result.columns.slice(0,4)) rather than all configured card properties; Cards ignores groupBy entirely (Cards component never reads result.groups), so a grouped cards view shows no group headers. |
| Bases editing (columns/filters/views) | 🟡 Partial | src/components/BaseView.tsx:BaseEditor (261-391) edits view name/type/limit, columns (add/remove/reorder via moveCol/addCol), a simple string filter, and add/delete views. Confirmed LIVE, not dead code: App.tsx:3083 passes onChange={(yaml)=>handleViewerChange(...)}; the '✎ Edit' button renders only when onChange is present (BaseView.tsx:210-218).<br>**Gap:** Auditor's gaps confirmed: property display-name editing not exposed (columns show def.display[k] read-only, 348/359); property type/widget config not editable; formula editing not exposed (formulas round-trip via raw); nested and/or/not filters shown read-only (380-382). ADDITIONAL missing editable dimensions the auditor omitted: sort order, groupBy, per-column summaries, and the cards image key are all preserved-but-not-editable in the UI (buildViews round-trips them via view.raw only). |

### Core-plugin equivalents

| Feature | Status | Where / gap |
|---|---|---|
| Daily notes | ✅ Full | src/App.tsx:1668 openDailyNote honors cfg.dailyNotesFormat/Folder/Template; src-tauri/src/lib.rs:1136-1161 reads .obsidian/daily-notes.json only when core-plugins.json shows daily-notes enabled; src/lib/daily.ts:formatMoment implements the Moment token subset with UnsupportedTokenError fallback; template filled via fillTemplate ({{date}}/{{time}}/{{title}}).<br>**Gap:** None material. Only 'Open today's daily note' exists (matching core; next/prev navigation is community Periodic Notes, not core). |
| Templates (insert with date/title) | 🟡 Partial | src/App.tsx:1387 insertTemplate + src/lib/templates.ts:applyTemplate supports {{title}},{{date}},{{time}},{{date:FMT}} and Templater <% tp.* %> tags (exceeds core). Folder read via src-tauri/src/lib.rs:1164-1175.<br>**Gap:** Obsidian core Templates plugin has three settings (folder, dateFormat, timeFormat) stored in .obsidian/templates.json; Basalt reads only 'folder' (lib.rs:1173). Bare {{date}}/{{time}} are hardcoded to YYYY-MM-DD/HH:mm (templates.ts:130, daily.ts:155-157), so a vault that changed the default date/time format gets silently different output than Obsidian. |
| Command palette (Cmd+P) | ✅ Full | src/components/Palette.tsx generic fuzzy palette (arrow/enter/esc nav); App.tsx:1301 Cmd+P → setModal('commands'); App.tsx:3310 renders every app command + plugin-contributed commands (App.tsx:2988) via fuzzyRank.<br>**Gap:** Palette rows show a static 'hint' string, not the user's actual assigned hotkey from the Hotkeys manager; no recently-used-command ordering and no pinned commands (Obsidian has both). |
| Outline pane | ✅ Full | src/components/Outline.tsx extracts ATX headings via proseMask (skips code/frontmatter), indents relative to shallowest level, click-to-jump onJump(line).<br>**Gap:** No collapse/expand of heading sections (Obsidian's outline has collapse arrows); no active/current-heading highlight tied to scroll; ATX-only, ignores Setext (underline === / ---) headings. |
| Word count | ✅ Full | src/App.tsx:2272 docStats computes words via text.match(/\S+/g) and chars=text.length; App.tsx:3212 renders status-count span with char count in the title tooltip.<br>**Gap:** No selection-based count (Obsidian shows 'N selected words/chars' when text is selected); /\S+/ tokenizer counts a space-free CJK run as one word, under-counting vs Obsidian's CJK-aware counter. |
| Note composer (merge/split, extract heading) | ❌ Missing | No merge/split/extract-to-new-note command anywhere. extractHeadings/extractBlockIds (transclude.ts) feed wikilink autocomplete; split-right/split-down (App.tsx:2960) are PANE splits, not note splits.<br>**Gap:** No 'Merge current file with another', 'Split note by headings', or 'Extract selection/heading into new note' commands. |
| Random note | ❌ Missing | No 'open random note' command; commands array (App.tsx:2925-2994) has none. 'random' hits are canvas ID crypto (CanvasView.tsx:60), graph layout, and a bases function (bases.ts:1270).<br>**Gap:** No feature to open a random note from the vault. |
| Slides / presentation mode | ❌ Missing | No reveal.js / slide / deck / presentation-mode code. 'present'/'slide' hits are all substrings (proseMask 'present', Ribbon 'presentational').<br>**Gap:** No slide-deck or presentation mode. |
| Audio recorder | ❌ Missing | No MediaRecorder/getUserMedia/microphone usage. 'record'/'recording' all refer to snapshot recording (snapshots.ts) and hotkey chord recording (SettingsModal.tsx). Media playback exists (media.ts) but is playback-only.<br>**Gap:** No audio/video capture like Obsidian's Audio recorder core plugin. |
| Custom hotkeys (remap commands) | 🟡 Partial | src/lib/hotkeys.ts chordOf/matchChord/loadBindings/saveBindings + localStorage; SettingsModal.tsx:169-187 record/clear UI; App.tsx:3003-3015 keydown runs matched command; App.tsx:3347-3354 enforces one-command-per-chord.<br>**Gap:** Cannot rebind or override any built-in shortcut: the custom handler bails on e.defaultPrevented (App.tsx:3005) while every built-in (Cmd+O/P, Shift+Cmd+F, Cmd+comma, Cmd+\, Cmd+W, Ctrl+Tab, zoom) preventDefaults first (App.tsx:1291-1348). Built-in defaults never appear in the Hotkeys manager (only palette + plugin commands are listed), and assigning a built-in chord to a command yields a silent dead key. No conflict warning against built-ins. |

### Settings, Theming & Appearance

| Feature | Status | Where / gap |
|---|---|---|
| Settings UI | 🟡 Partial | src/components/SettingsModal.tsx (256 lines): a single-scroll modal with Appearance (theme/readable-width/spellcheck), read-only Vault config, Hotkeys, CSS snippets, Plugins sections. Rendered from src/App.tsx.<br>**Gap:** Falls short of Obsidian's settings in three ways: (1) single-scroll modal vs Obsidian's tabbed multi-pane navigation; (2) plugin API (src/lib/plugins.ts:149-197) has NO addSettingTab, so plugins cannot register their own settings tabs — in Obsidian every community-plugin's settings live inside this modal; (3) vault settings (New links, Attachments, Daily notes, etc.) are DISPLAY-ONLY — SettingsModal.tsx:149-164 shows them 'read-only' and src-tauri never writes .obsidian config, whereas Obsidian lets you edit Files&Links / Editor settings here. |
| Light / dark / system theme | ✅ Full | src/lib/theme.ts: ThemeMode system\|light\|dark, resolveTheme() pure precedence, applyResolvedTheme() flips data-theme on <html>, watchSystemTheme() subscribes to OS prefers-color-scheme changes, editor dark-flag synced separately; no-flash inline script in index.html. UI seg-control at SettingsModal.tsx:117-130. styles.css:23/48 set color-scheme.<br>**Gap:** Does not support installing/selecting full community themes (Obsidian's cssTheme in appearance.json) — only light/dark/system + CSS snippets. That is arguably outside this specific feature's name, so not downgraded. |
| CSS snippets | 🟡 Partial | src/App.tsx:2206-2220 injects enabled snippets as <style data-basalt-snippet> tags; SettingsModal.tsx:195-221 toggle UI; disabled set persisted per-vault (basalt.disabledSnippets.<vault>). Backend src-tauri/src/lib.rs:1491-1496 reads them from '.basalt/snippets'.<br>**Gap:** Two shortfalls vs Obsidian: (1) snippets are read from .basalt/snippets, NOT .obsidian/snippets — an existing Obsidian vault's snippets are ignored, and enabledCssSnippets in .obsidian/appearance.json is not honored; (2) no live folder-watch, no 'Reload snippets' button, no 'open snippets folder' button — listCssSnippets only runs on vault change (App.tsx:2181-2205), so a newly added snippet file won't appear until the vault is re-switched/restarted. |
| Readable line length toggle | ✅ Full | SettingsModal.tsx:131-138 checkbox; App.tsx:320-325 localStorage 'basalt-readable-width'; App.tsx:3134 applies .readable-width class; App.tsx:2956 command 'toggle-readable-width'; styles.css:1962-1963 .readable-width .cm-content{max-width:720px} + .readable-width .reading-view{max-width:760px}, both centered.<br>**Gap:** (none material) — applies to both editor and reading view, has a checkbox and a palette command, persists. Auditor's line ref (1961-1962) is off by one (actual 1962-1963), trivial. |
| Font / appearance settings | ❌ Missing | No font-family/size UI anywhere in src (grep across src returns only CSS usages). Fonts are hard-coded CSS vars --font-text/--font-mono in styles.css:20-21. ObsidianConfig (vault.ts:140-148) does NOT include any font fields, and src-tauri/src/lib.rs never reads .obsidian/appearance.json (only app.json, core-plugins.json, daily-notes.json, templates).<br>**Gap:** No Appearance controls for interface/text/monospace font family or base font size to match Obsidian. Basalt also doesn't even read Obsidian's appearance.json to honor baseFontSize/fonts/accentColor/cssTheme. |
| Ribbon (left action bar) | 🟡 Partial | src/components/Ribbon.tsx: role='toolbar', aria-label, 5 top actions (sidebar/switcher/search/palette/graph) + Settings at bottom, each with tooltip+aria-label; styled at styles.css:2078-2103.<br>**Gap:** The ribbon is a FIXED hard-coded set of actions. Obsidian's ribbon is plugin-extensible (addRibbonIcon) and user-customizable (reorder/hide/manage via right-click). Basalt's plugin API (src/lib/plugins.ts:149-197) has no addRibbonIcon, and there is no user customization — the icon list is static. |
| Status bar | 🟡 Partial | src/App.tsx:3211-3227 renders word count (char count only in the title tooltip), save status (Saving…/Saved/Read-only), and a 'Changed on disk' conflict badge with Reload/Keep-mine. Styled at styles.css:285-289, 1995-1998.<br>**Gap:** Shortfalls vs Obsidian: (1) character count is hidden in a tooltip, not shown alongside word count (Obsidian shows both); (2) no backlink count / cursor position indicator; (3) NOT plugin-extensible — plugin API (src/lib/plugins.ts) has no addStatusBarItem, whereas Obsidian's status bar is heavily populated by plugins (vim mode, sync, backlink count, etc.). |

### Obsidian interop & data safety

| Feature | Status | Where / gap |
|---|---|---|
| Reads .obsidian config (link format, attachment folder) | ✅ Full | /Users/oweneldridge/projects/basalt/src-tauri/src/lib.rs:1124-1177 read_obsidian_config parses .obsidian/app.json for newLinkFormat, useMarkdownLinks, attachmentFolderPath; also honors daily-notes.json (gated on core-plugins.json enable state, both array+object schemas) and templates_folder (Templater data.json then core templates.json). read_obsidian_bookmarks (1230-1246) additionally flattens bookmarks.json. Read-only by design (never writes .obsidian/).<br>**Gap:** Scoped read: does not surface every interop-relevant Obsidian setting (e.g. alwaysUpdateLinks, wikilink vs md-link enforcement beyond the flag). attachmentFolderPath is read as a raw string; correct interpretation of './' note-relative syntax is a downstream concern, not this command's. |
| Same-vault live coexistence with Obsidian (fs watcher) | ✅ Full | /Users/oweneldridge/projects/basalt/src-tauri/src/lib.rs:1252-1329 start_watching (notify recommended_watcher, recursive, per-window emit_to, skips Metadata-only Modify events, .md->vault-changed / dirs+attachments->vault-rescan, build-then-swap watcher). Frontend: /Users/oweneldridge/projects/basalt/src/App.tsx:1076-1155 processChanges with CONTENT-based self-write suppression (selfWrites map, rememberSelfWrite 433-440, compared not consumed 1098-1100), plus real conflict badging when an external write hits a dirty pane (addConflict 1140/1144); pre-write conflict guard for viewers (544-550).<br>**Gap:** Relies on notify's platform backend; folder rename/delete is handled via coarse full-rescan rather than targeted diff. Concurrent external+local edits raise a conflict for the user to resolve rather than auto-merging (safe, but manual). |
| Atomic writes / no corruption on crash | ✅ Full | /Users/oweneldridge/projects/basalt/src-tauri/src/lib.rs:165-189 atomic_write: refuses symlinks, writes .basalt-tmp-* in same dir, sync_all (fsync) then fs::rename over target, removes temp on failure. sweep_temps (191-213) cleans stale temps on open_vault (284). Used by write_note/write_canvas/write_base (386,416,441).<br>**Gap:** Parent directory is NOT fsync'd after rename, so a crash immediately post-rename could lose the rename on some filesystems (durability gap) — but the reader always sees either the intact old or intact new file, never a truncated one, so the 'no corruption' claim holds. Symlinked notes are refused outright (Obsidian would follow them) — a minor behavioral deviation. |
| Plain-file format fidelity (md/canvas/base untouched) | 🟡 Partial | Extension-gated writers prevent cross-contamination (lib.rs:376-385 write_note .md-only, 401-415 write_canvas, 431-441 write_base; canvas/base must pre-exist). frontmatter.ts preserves un-edited keys verbatim (never rewrites a key the user didn't touch, 1-11,270-299). canvas.ts serializeCanvas (/Users/oweneldridge/projects/basalt/src/lib/canvas.ts:55-117) round-trips unmodeled fields via node.raw/passNodes/passEdges/topRaw and tab-indents like Obsidian. No autosave on mere open (only updateListener docChanged, setup.ts:285-290).<br>**Gap:** An open note edited in the editor is NOT byte-untouched: CodeMirror (setup.ts:292, no lineSeparator facet) normalizes CRLF->LF on serialize, so a CRLF file's body line endings change on the first body edit. Canvas geometry is rounded to integers on save (canvas.ts:73,85-88). These are the only deviations found. |
| CRLF / encoding preservation | 🟡 Partial | frontend nl detection exists (/Users/oweneldridge/projects/basalt/src/lib/frontmatter.ts:52,94-95,262 + frontmatter.test.ts round-trip tests) and rename/link rewrites string-replace on raw disk content (App.tsx:1926,2589,2807 readNote FRESH) so CLOSED files updated by link-rewrite keep CRLF. Backend is UTF-8 only: /Users/oweneldridge/projects/basalt/src-tauri/src/lib.rs:363 read_note uses fs::read_to_string (errors on non-UTF8, so a non-UTF8 note cannot even be opened for edit); vault listing (243) uses from_utf8_lossy (would corrupt on save).<br>**Gap:** (1) Non-UTF8 encodings unsupported (read_note errors; lossy listing). (2) CRLF is preserved only in narrow paths (unit tests, closed-file link rewrites); for any note OPENED and edited, CodeMirror (setup.ts:292, no lineSeparator) strips CRLF and frontmatter.ts's nl-preservation never engages because editor/frontmatter.ts:55 reads the already-LF-normalized CM doc. So editing an open note converts CRLF->LF. |
